name: Publish Docker images to GitHub Container Registry

on:
  push:
    branches: [ main ]

jobs:
  publish:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push honeypot-app image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.app
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/honeypot-app:latest

      - name: Build and push honeypot-exporter image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.exporter
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/honeypot-exporter:latest
