name: Publish Docker images to Docker Hub

on:
  push:
    branches: [ main ]

jobs:
  publish-dockerhub:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push honeypot-app to Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.app
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/honeypot-app:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/honeypot-app:${{ github.sha }}

      - name: Build and push honeypot-exporter to Docker Hub
        uses: docker/build-push-action@v5
        with:
          context: .
          file: Dockerfile.exporter
          push: true
          tags: |
            ${{ secrets.DOCKERHUB_USERNAME }}/honeypot-exporter:latest
            ${{ secrets.DOCKERHUB_USERNAME }}/honeypot-exporter:${{ github.sha }}
